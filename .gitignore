# Runtime artifacts & machine-local caches
build/
bin/
tmp/
core-bin/
aether-tor/
aether-masque*.toml
# WARP identities and gateway caches: machine-local credentials, never commit
aether.toml
aether-secondary.toml
*lastconn*.toml
aether-src.tar.gz
*.log
*.new
*.part
*.zip

# Reference copies & tooling, not part of the project
Aether-HEAD/
.codebuddy/
.dsh-vision-router/

# The installer's embedded payload placeholder must stay in the repo
# (release.ps1 swaps in the real bundle only at build time).
!cmd/aethersetup/payload/bundle.zip
